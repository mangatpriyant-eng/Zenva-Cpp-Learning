//#include <cstring>
//#include <iostream>

//int main() {
 // // int age = 18;
 // // int points = 1000;
 // // if (age >= 18)
 // // char rain = 'windscreen on';
 // // char rain = 'windscreen';
 // char rain = 'w';
 // int speed = 35;
 // // char combined[10];
 // char combined[50];
//  // if rain == 'windscreen';
 // // if (rain == 'windscreen');
 // // if (rain == 'w');
 // if (rain == 'w' && speed >= 35) {
   // // std::cout<< "slowdown" + "windscreen on" << std::endl;
  //  strcpy(combined, "slowdown ");      //  Copy "slowdown" into the empty space
  //  strcat(combined, "windscreen on");  //  Combine "windscreen on" with what's
                                        //  already in the space
   // std::cout << combined << std::endl; //  Print the combined word
   // // std::cout<< "slowdown" << strcat(rain,"windscreen on") << std::endl;
 // }
 // if (speed >= 35) {
   // std::cout << "slowdown" << std::endl;
   // std::cout << "windscreen on" << std::endl;
 // }
  // if (rain == 'w') {

   // // std::cout << "windscreen on" << std::endl;
 // } else if (rain == 'c') {
  //  std::cout << "no need for put windscreen on" << //std::endl;
 // }
//  //}

  // int main(){
 // char wether = 'z';
 // if (wether == 'z') {
 //   std::cout << "brakelights on" << std::endl;
 // } else
  //  wether = 'b';
 // std::cout << "brakelights off\n";
 // if (speed <= 35) {
  //  std::cout << "speed up" << std::endl;
 // } else
   // speed = 40;
  //std::cout << "right speed" << std::endl;
 // // else if (speed = 60)
 // else if (wether == 'b' && speed == 60) {
// // else if (wether == 'b' && speed == 60) {
   //  //if (speed = 60) 
 //     // if (speed >= 60) 
    // {
   //    std::cout << "too fast press the brakes now" << std::endl;
 //    }
    //std::cout << "slowdown"<< std::endl;
//  }
//  else if (wether != 'b' && wether != 'w') {
//    rain = 'c';
//  }
//}